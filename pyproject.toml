[project]
name = "virtualptz-runner-agent"
version = "0.1.0"
description = "Linux runner agent for VirtualPTZ external execution"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.115.0",
  "uvicorn[standard]>=0.30.0",
]
